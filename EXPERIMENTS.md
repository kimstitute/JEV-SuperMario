# 최초 실행 기록 — 2026-09-20

기존 비교의 공통 조건: SuperMarioBros-1-1-v0, seed=0, Jev 1.13.0, 7개 행동, 결정당 6게임 프레임, 최근 4관측(4프레임 간격), API 대기 중 게임 일시 정지. 실행 중 정책의 행동을 수정하지 않았습니다. 비정지 `realtime` 검증은 문서 끝에 별도로 기록하며, 스모크 테스트는 성능 비교에서 제외합니다.

## 연결 확인

`runs/20260920-142044-jev`: 3회 API 호출, 오른쪽 달리기 정상 실행. 입력 4,107토큰, 추정 $0.000172494. 그 전에 샌드박스 네트워크 차단으로 실패한 실행은 별도 폴더에 남아 있습니다.

## 좌표 입력

`runs/20260920-142208-jev`: 18회 판단, 106게임 프레임, x=40 → 최대 x=315(275px 전진), 첫 굼바 접촉 후 종료. 평균 API 지연 0.297초, 입력 38,837토큰, 추정 $0.001631154.

마지막 관측에서 굼바 후보의 위치와 접근이 검출되어 있었습니다. Jev는 RIGHT_B를 계속 골랐습니다. 관측에서 적이 사라져서 발생한 실패는 아니지만, 다른 관측 노이즈와 좌표 해석의 영향을 분리한 결과는 아닙니다.

## 사실 문장 입력 — 중간 디버깅 실행

`runs/20260920-142346-jev`: 21회 판단, 126게임 프레임, 260px 전진 후 사망. 평균 API 지연 0.269초, 입력 34,664토큰, 추정 $0.001455888.

관측 문장 생성기의 지하 타일 생략 조건이 마리오가 공중에 있을 때 바닥 전체를 생략하는 버그를 발견했습니다. 이 실행은 결함이 있는 중간 결과이며, 이를 정상 입력 형식의 성능으로 취급하지 않습니다. 회귀 테스트를 추가하고 수정했습니다.

## 사실 문장 입력 — 수정 후 실행

`runs/20260920-142556-jev`: 21회 판단, 126게임 프레임(약 2.1게임 초), x=40 → 최대 x=300(260px 전진), 첫 굼바에서 사망. 평균 API 지연 0.279초, 입력 36,853토큰, 추정 $0.001547826.

프레임 72에서 RIGHT_A를 선택했고 이후 점프 버튼을 해제했습니다. 굼바에 도달하기 전에 상승/하강이 진행됐고 접촉을 피하지 못했습니다. 점프 행동이 생겼다는 관찰은 가능하지만, 좌표 입력보다 성능이 개선됐다는 결론은 내릴 수 없습니다.

## 범위와 다음 실험

이 기록의 목적은 Jev-only 제어 루프를 실제로 연결하고, 실패한 입력과 행동을 검토할 수 있게 만드는 것입니다. 클리어는 달성하지 못했습니다.

후속 비교에서는 관측기를 고정하고 행동 간격(2/4/6프레임), 입력 표현, 여러 반복 실행을 비교하는 것이 적절합니다. 모델 변경, 입력 변경, 관측기 수정을 한꺼번에 적용한 결과를 입력 효과로 해석하면 안 됩니다. 성능 개선을 위해 점프 규칙이나 예측 궤적을 추가하면 별도의 보조 제어 조건으로 기록해야 합니다.

## 문자 화면 입력

`runs/20260920-142920-jev`: 동일한 RGB 검출 결과를 8픽셀 단위의 32×25 문자 격자로 바꾸고, 최근 격자 4개와 마리오 자기 상태만 전달했습니다. 적의 개별 좌표나 장면 설명은 전달하지 않았습니다. 격자 범례와 읽는 방법은 요청에 명시했습니다.

18회 판단, 106게임 프레임, 275px 전진 후 첫 굼바에서 사망. 평균 API 지연 0.287초, 입력 34,037토큰, 추정 $0.001429554. 이 실행에서는 RIGHT_B만 선택했습니다. 격자를 입력하는 기능은 작동하지만, 그 자체로 회피 동작이 개선되지는 않았습니다.

격자는 이미지를 그대로 토큰화한 것이 아니라 화면 인식기의 출력입니다. 인식 누락/오류와 8픽셀 양자화 영향을 포함합니다. 격자용 설명을 공통 질문 지침에 추가했으므로 엄밀한 입력 형식 비교는 같은 버전의 질문으로 재실행해야 합니다.

총 성공 응답: 81회. 총 입력 토큰: 148,498. 공개 단가 기준 추정 합계: $0.006236916. 실제 청구 금액은 서비스 계정에서 확인해야 합니다.

## 지속 관측·지형 기억 적용 후 — 2026-09-20

`runs/20260920-145306-jev`: 18회 판단, 106게임 프레임, 275px 전진 후 첫 굼바에서 사망. 평균 API 지연 0.299초, 입력 76,756토큰, 추정 $0.003223752. 전체 실행 시간 22.164초이며 실시간 60fps 처리 결과가 아닙니다. 모든 행동은 Jev의 `RIGHT_B`였습니다.

이번 입력은 지형·물체·관측 상태를 분리한 격자와 추적 좌표/속도를 함께 전달합니다. 이전 문자 격자 단독 실험과 입력량 및 표현이 다르므로 직접적인 성능 비교로 해석하지 않습니다.

초기 화면을 포함하여 **107개 프레임을 빠짐없이 관측**했습니다. 첫 굼바는 같은 ID `v1`로 추적됐고, 마지막 결정(프레임 102)에서 상대 x=14px, 월드 vx=-0.75px/frame으로 현재 관측에 포함됐습니다. 바닥 층은 적 아래까지 이어져 있으며 가림은 별도 층에 표시됩니다. 검출 누락으로 만든 바닥 구멍은 없었습니다. 관측 개선은 확인했지만 이번 재실행에서 회피 행동 개선은 확인하지 못했습니다.

별도 진단 `runs/20260920-144849-smoke`에서는 298게임 프레임/299관측으로 굼바 두 보행 모양과 파이프 윗부분·몸통을 확인했습니다. 스크립트 행동으로 x=693까지 간 결과이며 Jev 성능에 포함하지 않습니다. 사용된 블록·계단, 블록 제거, 가림 뒤 복원 등은 합성 회귀 장면으로 검증했습니다. 실제 계단까지 통과했다는 주장이 아닙니다.

추가 회귀 테스트를 포함해 23개 통과. 누적 성공 API 응답은 99회, 누적 입력 225,254토큰, 누적 추정 비용 $0.009460668입니다.

## 규칙 분석 + 분리된 Jev 판단 — 2026-09-20

`runs/20260920-151250-jev`: `--controller composed --decisions 120`으로 실행했습니다. 같은 RGB 관측기에 기하·접근 시간·점프 구간 분석을 추가하고, 한 API 요청에서 이동 Choice와 점프 시작/유지 Noul을 평가합니다. 지상/공중 및 A 해제 여부로 사용할 답을 고르고, Noul 임계값은 0.5입니다. 왼쪽+점프 조합을 추가했으며 착지 시에는 최대 6프레임을 기다리지 않고 다음 결정을 요청합니다.

결과: **89회 API 요청, 499게임 프레임, x=40 → 1130(1,090px 전진)**. 첫 굼바와 여러 파이프를 통과한 후 첫 낭떠러지에서 사망했습니다. 입력 522,994토큰, 출력 7,565토큰, 추정 비용 $0.021965748, 평균 API 지연 0.285초, 실행 시간 103.173초입니다. 클리어 결과는 아닙니다.

첫 점프는 프레임 90에서 `start_jump=0.51`에 따라 `RIGHT_A_B`로 실행됐습니다. 프레임 461/467/473에는 확인된 낭떠러지가 분석에 포함되고 접근 신호가 참이었지만, 시작 확률은 각각 0.28/0.26/0.34로 임계값에 미달했습니다. 프레임 479부터 공중 상태가 되어 새 점프를 시작할 수 없었습니다. 이 구간은 관측 누락보다 분석을 해석하는 질문/판단의 개선이 필요한 사례입니다.

500개 프레임 관측의 연속성을 확인했습니다. 89개 기록 모두 저장된 Jev 응답을 다시 조합하면 실제 실행 행동 및 `composition`과 일치합니다. 회귀 테스트는 **42개 통과**했습니다. 원문 질문, 세 응답, 조작 조건, 분석은 리플레이에서 확인할 수 있으며 첫 점프 입력은 `first-jump-request.json`에 저장했습니다.

이번 실행은 분석·질문·착지 결정 주기가 함께 바뀐 단일 탐색 실행입니다. 이전 275px 결과보다 멀리 갔지만, 평균 성공률이나 특정 변경의 인과적 효과를 입증하지 않습니다. 규칙은 화면 관측을 가공하며 별도 LLM, 적/맵 RAM, 에뮬레이터 미래 상태 탐색, 자동 회피 대체 정책은 사용하지 않았습니다.

누적 성공 API 요청은 188회, 누적 입력 748,248토큰, 누적 추정 비용 $0.031426416입니다.

## 행동 후보 평가 + 결합된 선택 — 2026-09-20

새 기본값 `--controller candidates`는 한 결정에 두 요청을 사용합니다. 첫 요청에서 완전한 버튼 조합별 생존·회복 가능성을 Noul로 평가하고, 두 번째 요청에 실제 평가 응답을 넣어 최종 행동을 Choice로 선택합니다. 규칙은 지지면 상실 시간 범위, 제동 거리, 수평 이동 범위, 착지면까지 필요한 이동량을 제공하고 위험 구간에서 판단 간격을 1~2프레임으로 줄입니다. 수평 예측은 충돌을 무시한 초기 근사이며, 숨은 맵이나 에뮬레이터 미래 실행을 사용하지 않습니다.

### 저장된 장면 비교

`runs/candidate-saved-state-eval-v2`: 이전 composed 실행의 현재 관측과 해당 시점 **이전** 행동만 입력했습니다. 다섯 장면, 10 API 요청, 61,643 입력 토큰, 추정 $0.002589006입니다.

| 이전 실행 프레임 | 이전 행동 | 새 행동 | 새 실행 간격 |
|---|---|---|---|
| 0 | RIGHT_B | RIGHT | 6 |
| 90 | RIGHT_A_B | LEFT | 6 |
| 461 | RIGHT_B | RIGHT_A | 6 |
| 467 | RIGHT_B | RIGHT_A | 2 |
| 473 | RIGHT_B | RIGHT_A_B | 1 |

낭떠러지 앞 세 장면에서 점프 응답으로 바뀌었습니다. 프레임 90은 제동으로 바뀌었지만 이것만으로 더 안전하다고 판단할 수 없습니다. 저장 상태 평가에서는 실제 다음 장면이 생성되지 않으므로 성공률이나 통과 결과가 아닙니다.

### 점프 유지 회귀와 보완

초기 후보 버전 `runs/20260920-153408-jev`는 첫 굼바를 넘었으나 파이프 앞에서 A를 2프레임 만에 해제하고 낮은 점프를 반복했습니다. 85회 결정, 최대 x=436(396px 전진), 877,186 입력 토큰 후 API 연결 오류로 종료됐습니다. 171회 요청 시도 중 170회 응답을 받았으며, 오류 뒤 행동을 실행하거나 자동 재시도하지 않았습니다. 추정 비용 $0.036841812입니다.

`candidate-v2`는 상승 중 A 유지가 새 점프가 아니라 기존 상승의 지속이라는 점, 해제하면 착지 전까지 높이를 되돌릴 수 없다는 점을 후보별로 명시합니다. 장애물 위로 더 올라가야 하는 높이와 최근 완료된 점프의 관측 높이·A 유지 길이를 추가했습니다. `runs/candidate-pipe-eval-v2`에서 이전 프레임 228의 해제 선택이 A 유지로 변경됐습니다. 프레임 230은 이미 A를 해제한 상태이므로 새 점프 후보를 만들지 않습니다. 3개 장면, 6회 요청, 34,477 입력 토큰, 추정 $0.001448034입니다.

### v2를 처음부터 실행

`runs/20260920-153841-jev`: 최대 120결정/150만 입력 토큰으로 실행했습니다. **118개 행동, 508게임 프레임, x=40 → 837(797px 전진)** 후 토큰 상한으로 정상 중단했습니다. 첫 굼바와 여러 파이프를 통과했지만 첫 낭떠러지에는 도달하지 않았습니다. 레벨을 클리어하거나 기존 전진 기록을 갱신한 결과가 아닙니다.

API 237회, 입력 1,503,865토큰, 출력 23,648토큰, 추정 $0.06316233. 요청당 평균 0.293초, 결정당 평균 0.589초, 전체 실행 156.045초입니다. 119번째 결정의 후보 평가 뒤 토큰 상한을 넘어 최종 선택 요청과 행동 실행을 중단했습니다. 이 부분의 응답과 비용도 `api.jsonl` 및 요약에 포함됩니다.

509개 연속 관측, 118개 최종 Choice와 실제 버튼의 일치, 모든 실행 간격이 요청 간격 이하임을 검사했습니다. 47개 결정의 간격이 6프레임보다 짧았습니다. 회귀 테스트는 **59개 통과**했습니다. 새 방식은 판단 간 의존 관계와 점프 유지를 개선했지만, 두 요청과 보수적인 이동으로 토큰·호출량이 늘었습니다. 이전 방식보다 비용 대비 전진 성능이 좋아졌다고 볼 근거는 없습니다.

### 낭떠러지 직전 장면을 실제 게임에서 재현

`runs/20260920-164440-matched-probe`: 이전 composed 실행의 프레임 461까지 실제 과거 버튼만 재생했습니다. 그 시점의 **RGB 전체와 마리오 자기 상태가 원본과 정확히 일치**함을 확인하고 새 컨트롤러를 시작했습니다. 설정 구간은 새 컨트롤러 성능으로 집계하지 않습니다. 해당 시점 이후의 이전 실행 결과는 정책 입력에서 제외했습니다.

새 Jev는 RIGHT_A로 점프를 시작하고 이후 다섯 결정에서도 A를 유지했습니다. 첫 낭떠러지를 건너 반대편 지면에 착지했고, 30회 결정 한도까지 생존한 채 x=1051 → 1304(**253px 추가 전진**)했습니다. 새 제어 구간은 152게임 프레임, API 60회, 입력 425,093토큰, 추정 $0.017853906입니다. 153개 연속 관측과 모든 최종 Choice·실행 버튼의 일치를 검사했습니다.

동일 장면에서 기존의 점프 누락을 해결한 한 번의 실제 게임 결과입니다. 전체 레벨 완주나 다양한 시작 상태에서의 성공률을 입증하는 결과는 아닙니다. 새 기본 컨트롤러의 전체 실행은 여전히 비용과 보수적인 진행 속도가 개선 과제로 남습니다.

참고: 최초 저장 상태 평가와 최초 장면 재현 시도는 샌드박스의 네트워크 차단으로 API 응답 없이 중단됐습니다. 성공 결과와 구분해 해당 실행 폴더도 보존했습니다.

## 공통 게임 규칙 전달 수정과 통제 비교 — 2026-09-20

계획을 `docs/rule-prompt-plan.md`에 먼저 기록했습니다. 관측기, 후보 생성, 물리 근사, 적응형 프레임 간격, 실행 조합 코드는 고정했습니다. Nintendo 원본 SMB 설명서의 버튼/적 접촉/낭떠러지 규칙을 확인하고, 게임 동작·관측 해석·이 컨트롤러의 조작 제한·판정 기준을 분리했습니다. 중앙 `rulebook.py`의 내용을 두 단계의 실제 요청에 넣고 모든 질문이 명시적으로 참조하게 했습니다. 독립적인 system 메시지 필드를 새로 만든 것은 아닙니다.

실험군:

- `legacy-v2`: 변경 전 질문을 별도 모듈로 동결.
- `rules-only-v1`: 기존 질문과 criteria를 유지하며 공통 게임/관측/조작 규칙과 참조 지시만 추가.
- `rules-v1`: 같은 공통 규칙에 최대 48게임 프레임의 국소 회복 가능성 판단 기준과 생존/전진/회복 우선순위를 추가. 실제 실행 간격은 그대로 1~6프레임.

### 고정 상태 비교

`runs/rule-prompt-comparison-v1`: 미리 선정한 10장면 × 3프로필 × 2회 = 60결정/120 API 요청. 프로필 순서를 번갈아 실행했습니다. 모든 대응 입력의 SHA256이 일치하며 현재 결정 이후 결과는 입력하지 않았습니다. 재평가용 입력 10개를 `experiments/fixtures`에 보관해 과거 runs가 없어도 비교를 재현할 수 있습니다.

| 장면 | legacy-v2 | 규칙만 추가 | 규칙+판정 기준 |
|---|---|---|---|
| 평지 | RIGHT ×2 | RIGHT ×2 | RIGHT ×2 |
| 낮은 천장 아래 적 접근 | LEFT ×2 | RIGHT_A / LEFT | RIGHT_A ×2 |
| 파이프 접근 | RIGHT_A ×2 | RIGHT_A ×2 | RIGHT_A ×2 |
| 파이프 앞 상승·A 유지 중 | RIGHT_A ×2 | RIGHT_A ×2 | RIGHT_A ×2 |
| 파이프 앞 이미 A 해제 | RIGHT ×2 | RIGHT ×2 | RIGHT ×2 |
| 낭떠러지 접근 | RIGHT_A ×2 | RIGHT_A_B ×2 | RIGHT_A_B ×2 |
| 낭떠러지 가까이 | RIGHT_A_B ×2 | RIGHT_A_B ×2 | RIGHT_A ×2 |
| 낭떠러지 직전 | RIGHT_A_B ×2 | RIGHT_A_B ×2 | RIGHT_A ×2 |
| 낭떠러지 위 하강 | RIGHT ×2 | RIGHT ×2 | RIGHT_A ×2 |
| 낭떠러지 통과 후 접지 | RIGHT ×2 | RIGHT ×2 | RIGHT_A ×2 |

행동 변화는 정답률이 아닙니다. 특히 rules-only의 적 접근 장면은 두 번의 답이 달랐습니다. rules-v1은 하강·착지 뒤에도 A를 더 선택했으므로 무조건 개선으로 해석하지 않았습니다. 행동 차이를 실제 구간 실행에서 확인했습니다.

입력 토큰: 대조군 244,567 / 규칙만 281,385 / 규칙+기준 285,794. 공통 규칙만 추가하면 이 세트에서 약 15.1%, 전체 수정은 약 16.9% 증가했습니다. 비교 전체 811,746 입력 토큰, 추정 비용 $0.034093332.

### 실제 게임에서 같은 장면 비교

같은 과거 버튼을 실제 에뮬레이터에서 재생하고 **원본 RGB 전체와 마리오 상태가 정확히 일치**할 때 시작했습니다. 두 프로필의 첫 입력 SHA256도 일치합니다. 각 실행은 새 제어 20결정/40 API 요청이며 초기 재현 구간은 전진 성능에서 제외했습니다.

| 시작 장면 | 프로필 | 새 게임 프레임 | 추가 전진 | 결과 | 입력 토큰 |
|---|---|---:|---:|---|---:|
| 낭떠러지 접근, frame 461 | legacy-v2 | 92 | 197px | 통과·생존 | 266,042 |
| 같은 장면 | rules-v1 | 80 | 225px | 통과·생존 | 300,901 |
| 파이프 앞 상승, frame 228 | legacy-v2 | 76 | 133px | 통과·생존 | 230,232 |
| 같은 장면 | rules-v1 | 76 | 133px | 통과·생존 | 271,580 |

실행 폴더는 각각 `rule-ab-gap-legacy-v2-network`, `rule-ab-gap-rules-v1-network`, `rule-ab-pipe-legacy-v2`, `rule-ab-pipe-rules-v1`입니다. 파이프 통과 이후에는 수정 버전이 점프를 더 선택했지만 이번 한도 내에서 전진 거리는 같았습니다. 낭떠러지에서는 더 전진했지만 실제 진행 프레임 수도 다르므로 반복 성공률이나 프롬프트 일반 성능의 우위를 뜻하지 않습니다. 처음 두 낭떠러지 시도는 샌드박스 네트워크 차단으로 응답 없이 중단됐으며 별도 폴더로 보존했습니다.

### 결론과 기본값

**공통 규칙 전달의 누락을 수정했다는 것은 확인했습니다. 기존 실패의 원인이 모두 규칙 부족이었다는 가설은 입증되지 않았습니다.** 일부 행동은 달라졌고 두 구간의 통과를 보존했습니다. 기본 질문 프로필은 `rules-v1`이며 대조군과 rules-only를 CLI로 재현할 수 있습니다. 전체 레벨 완주·성공률 비교는 이번 검증 범위가 아닙니다.

전체 회귀 테스트 **70개 통과**. 네 실제 실행의 연속 관측, 두 단계 동일 규칙, 최종 Jev Choice와 실제 행동 일치를 검사했습니다. 리플레이에 공통 규칙·판정 기준과 실제 전송 state를 표시합니다. 한 요청에 규칙을 추가했다는 사실이 이후 요청에도 자동 적용된다고 가정하지 않습니다.
## 단일 호출·비정지 컨트롤러

기존 `candidates` 경로는 후보별 Noul 평가와 최종 Choice를 순서대로 요청해 한 결정에 두 번 호출하고, 응답을 기다리는 동안 게임을 멈췄다. 이를 유지한 채 `--controller realtime`을 추가했다. 이 경로는 후보의 결정론적 결과를 하나의 Choice 상태로 보내며, API 요청은 결정마다 정확히 한 번이다. 별도 워커가 요청을 수행하는 동안 에뮬레이터는 60fps로 진행하고, 응답이 도착하면 다음 프레임부터 새 버튼을 적용한다. 첫 응답 전에는 중립 `NOOP`을 사용한다.

`runs/realtime-smoke-20260920-c`에서 10회 결정·10 API 호출을 확인했다. API 지연 동안 총 24게임 프레임이 진행됐고, 평균 응답 지연은 약 0.28초 수준이었다. 입력 상태와 응답 시점의 프레임 차이는 각 결정의 `realtime.stale_state_frames`에 기록한다. 이 짧은 검증은 비정지·단일 호출 동작만 확인한 것이며, 게임 성공률 비교 결과는 아니다.

이후 `realtime-v2` 로그 스키마를 추가했다. `runs/realtime-log-smoke-20260920`에서 10게임 프레임에 대해 `frames.jsonl`·`perception.jsonl` 11개(초기 프레임 포함), 결정 3개, API 이벤트 6개(request/response)를 서로 대조했다. 각 결정에는 요청 시점 state의 SHA256과 `stale_state_frames`가 있다.
